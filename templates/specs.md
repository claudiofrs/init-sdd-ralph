# Specs — Roadmap Index

One epic active at a time, each living in `specs/<epic-name>/`. If the project has a longer
multi-phase plan, keep it in `specs/trd.md` and reference it here.

| # | Epic | Dir | Status | Stories |
|---|------|-----|--------|---------|
| 1 | {{DEFAULT_EPIC}} | `specs/{{DEFAULT_EPIC}}/` | planned | not yet spec'd |

Run `./scripts/ralph/ralph.sh --list` for live pass counts per epic with a `prd.json`.

## Open

- Each future epic gets its own `specs/<epic-name>/` directory (`spec.md`, `plan.md`,
  `prd.json`, …) when it becomes active — not written upfront for the whole roadmap.

## Future-iteration backlog (not scheduled, not reopened)

- Park deliberately-deferred work here with the date and reason it was deferred, so it isn't
  silently forgotten or accidentally reopened mid-roadmap.
